{
  "name": "modern-notepad",
  "version": "1.0.0",
  "description": "A beautiful modern notepad with 3D heart bubbles and gendered themes",
  "scripts": {
    "build": "cd server && npm install && cd ../client-vite && npm install && npm run build && cd ..",
    "start": "cd server && NODE_ENV=production node server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}